// my-theme.ts
import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  mainTextColor: 'whitesmoke',
  boxTextColor: 'black',
  boxColor: 'whitesmoke',
};

export { theme };
